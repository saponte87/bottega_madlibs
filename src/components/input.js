import React from 'react';

const Input = (title, content) => {
    return (
        <div className="input">
            <input />
            <label>{title}</label>
        </div>
    )
}

export default Input;